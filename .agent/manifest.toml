[repo]
name = "agentjj"
description = ""
languages = []
vcs = "jj"

[entry_points]

[interfaces]

[invariants]
format = { cmd = "cargo fmt --check", on = ["pre-commit"] }
lint = { cmd = "cargo clippy -- -D warnings", on = ["pre-commit", "pre-push"] }
test = { cmd = "cargo test", on = ["pre-push"] }

[permissions]
allow_change = []
deny_change = []
allow_push = []
deny_push = []

[branches]
trunk = "main"
protected = []

[review]
require_human = []
